# T07 | Literal plus bounded structural search

**Product invariant:** Think-wide supplies cross-project evidence/decisions/briefs. It does not repair or execute source projects. Trusted bounded read-only analysis remains allowed. Exact references and current authorization govern every surface.

**Evidence rule:** Documentation/fixtures, installed APIs, local real handlers, live provider/host, and deployment are separate statuses. No pass without an actual result at the identified commit/config.

## Issuance

Suggested specialty: **Andrew**. Actual owner: unclaimed until the PM assigns. Class: core.

Attempt timebox: **35 minutes**, including the first compatibility probe. Hard stop: **September 20, 2026, 14:15 EDT**, or earlier relevant feature cutoff. These bound effort; they are not promised completion times.

Prerequisite seed IDs: T02, T04. Before READY, replace these with actual integrated commits/artifacts and record the contract version. No stale issue label is sufficient.

Writable areas: src/server/analysis/, skills/ trusted recipe content, tests/structural/. Confirm actual paths against the new checkout. Shared contract/lock/auth/infra changes need the designated owner and reviewer.

## Goal

Implement or pair on a fixed-string search and a small ast-grep recipe path. Treat matches as structural evidence, not proven semantics.

## Done means

Known matching/nonmatching snippets; JSON ranges map to exact source refs; limits and parse failures visible; no rewrite/plugin/shell input; final scan runs on T05 snapshots before completion.

## Stop and fallback

Stop at the timebox or hard stop, or after about ten minutes of unsupported API guessing. Preserve the branch and explain the concrete blocker before changing tasks.

Keep literal search complete and explicitly mark AST unsupported if its binary/profile cannot be proven. Do not quietly relabel text search structural. Record the missing structural capability.

## Review and handoff

Review partner: Marc. Provide branch/commit, actual changed behavior, exact test commands/results, local/live/mock status, contract/config, remaining gaps, and next useful action. DONE requires acceptance and integration into trunk; otherwise REVIEW or BLOCKED.

Can start on trusted rule fixtures before T05 finishes. Closure additionally needs T05 source refs and actual snapshot scan. Free Marc/Eassa may implement the adapter while Andrew owns its independent rule/effect tests.

## Read only when needed

[Product architecture](../02_PRODUCT_AND_ARCHITECTURE.md) · [Tools and map](../03_TOOLS_AND_MAP.md) · [Build contract](../04_BUILD_CONTRACT.md) · [Security/tests](../05_SECURITY_AND_CI.md) · [Clock](../01_PHASE0_AND_CLOCK.md).
