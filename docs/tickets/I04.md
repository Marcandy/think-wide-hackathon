# I04 | GalaxyGate deployment and one actual remote MCP host

**Product invariant:** Think-wide supplies cross-project evidence/decisions/briefs. It does not repair or execute source projects. Trusted bounded read-only analysis remains allowed. Exact references and current authorization govern every surface.

**Evidence rule:** Documentation/fixtures, installed APIs, local real handlers, live provider/host, and deployment are separate statuses. No pass without an actual result at the identified commit/config.

## Issuance

Suggested specialty: **Eassa**. Actual owner: unclaimed until the PM assigns. Class: integration.

Attempt timebox: **35 minutes**, including the first compatibility probe. Hard stop: **September 20, 2026, 15:30 EDT**, or earlier relevant feature cutoff. These bound effort; they are not promised completion times.

Prerequisite seed IDs: T08, I01. Before READY, replace these with actual integrated commits/artifacts and record the contract version. No stale issue label is sufficient.

Writable areas: infra/, src/server deployment-only wiring, host compatibility receipts. Confirm actual paths against the new checkout. Shared contract/lock/auth/infra changes need the designated owner and reviewer.

## Goal

Deploy the already integrated application with private Convex/admin boundaries and connect one real ChatGPT or Claude.ai client. Use a standard reviewed HTTPS deployment, not a weakened tunnel to demo auth.

## Done means

HTTPS/auth works; intended private ports stay private; real host logs in then browses/searches/reads exact evidence and retrieves a decision/brief; no direct-backend bypass; host/version/profile recorded.

## Stop and fallback

Stop at the timebox or hard stop, or after about ten minutes of unsupported API guessing. Preserve the branch and explain the concrete blocker before changing tasks.

Keep local product development and recording. Mark hosted deployment and remote browser-host connection incomplete. A local MCP pass cannot substitute for this ticket.

## Review and handoff

Review partner: Andrew. Provide branch/commit, actual changed behavior, exact test commands/results, local/live/mock status, contract/config, remaining gaps, and next useful action. DONE requires acceptance and integration into trunk; otherwise REVIEW or BLOCKED.

Reserve an early slot; do not defer first host attempt until the last hour. Public ingress opens only with real auth. Parser and tool paths still enforce source scope. Embedded MCP App UI is an extra check, not prerequisite to plain tool use.

## Read only when needed

[Product architecture](../02_PRODUCT_AND_ARCHITECTURE.md) · [Tools and map](../03_TOOLS_AND_MAP.md) · [Build contract](../04_BUILD_CONTRACT.md) · [Security/tests](../05_SECURITY_AND_CI.md) · [Clock](../01_PHASE0_AND_CLOCK.md).
