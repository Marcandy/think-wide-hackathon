# T05 | Two Git snapshots, navigable map, and exact reads

**Product invariant:** Think-wide supplies cross-project evidence/decisions/briefs. It does not repair or execute source projects. Trusted bounded read-only analysis remains allowed. Exact references and current authorization govern every surface.

**Evidence rule:** Documentation/fixtures, installed APIs, local real handlers, live provider/host, and deployment are separate statuses. No pass without an actual result at the identified commit/config.

## Issuance

Suggested specialty: **Marc**. Actual owner: unclaimed until the PM assigns. Class: core.

Attempt timebox: **40 minutes**, including the first compatibility probe. Hard stop: **September 20, 2026, 13:30 EDT**, or earlier relevant feature cutoff. These bound effort; they are not promised completion times.

Prerequisite seed IDs: T02. Before READY, replace these with actual integrated commits/artifacts and record the contract version. No stale issue label is sufficient.

Writable areas: src/server/sources/, convex/repositories and entries, exact-reference core helpers. Confirm actual paths against the new checkout. Shared contract/lock/auth/infra changes need the designated owner and reviewer.

## Goal

Load two permitted local Git snapshots or an already authorized provider source. Build bounded tree metadata and exact source retrieval without executing target code.

## Done means

Two actual commit/tree identities; browse immediate children; exact ranges/digests match original bytes; coverage and missing-source states work; source refs reach the shared domain boundary.

## Stop and fallback

Stop at the timebox or hard stop, or after about ten minutes of unsupported API guessing. Preserve the branch and explain the concrete blocker before changing tasks.

Freeze a blocked live provider connector. Use selected immutable local Git snapshots with truthful provenance, not invented historical evidence.

## Review and handoff

Review partner: Andrew. Provide branch/commit, actual changed behavior, exact test commands/results, local/live/mock status, contract/config, remaining gaps, and next useful action. DONE requires acceptance and integration into trunk; otherwise REVIEW or BLOCKED.

Use safe Git object reading or equivalent provider APIs, not target hooks, submodules, filters, or package installs. Coordinate reference shape changes with T02.

## Decided since this seed was written

T05 adds `contracts/schemas/commit-record.schema.json` and sets `entries` for `readHistory`; the `readSource` handler rejects inverted byte/line ranges with `invalid_request`; T05 writes a `snapshot` grant row when it registers a snapshot, because T06 authorizes snapshots through `grants` ([decisions/0001](../decisions/0001-contract-source.md), [0002](../decisions/0002-operation-pipeline.md)).

## Read only when needed

[Product architecture](../02_PRODUCT_AND_ARCHITECTURE.md) · [Tools and map](../03_TOOLS_AND_MAP.md) · [Build contract](../04_BUILD_CONTRACT.md) · [Security/tests](../05_SECURITY_AND_CI.md) · [Clock](../01_PHASE0_AND_CLOCK.md).
