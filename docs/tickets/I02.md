# I02 | Ergonomic selected-repository GitHub connection

**Product invariant:** Think-wide supplies cross-project evidence/decisions/briefs. It does not repair or execute source projects. Trusted bounded read-only analysis remains allowed. Exact references and current authorization govern every surface.

**Evidence rule:** Documentation/fixtures, installed APIs, local real handlers, live provider/host, and deployment are separate statuses. No pass without an actual result at the identified commit/config.

## Issuance

Suggested specialty: **Marc**. Actual owner: unclaimed until the PM assigns. Class: optional.

Attempt timebox: **35 minutes**, including the first compatibility probe. Hard stop: **September 20, 2026, 14:15 EDT**, or earlier relevant feature cutoff. These bound effort; they are not promised completion times.

Prerequisite seed IDs: T05, I01. Before READY, replace these with actual integrated commits/artifacts and record the contract version. No stale issue label is sufficient.

Writable areas: src/server/providers/github/, connector UI, grant/provenance adapter. Confirm actual paths against the new checkout. Shared contract/lock/auth/infra changes need the designated owner and reviewer.

## Goal

Wire a managed selected-repository GitHub App connection to the existing source contract. Eassa supplies/reviews credential boundaries; Marc implements product-side calls.

## Done means

Connect/select/read works; installation/repo substitution denied; exact provider snapshot/byte refs; short-lived credentials not in browser, prompts or parser; disconnect stops future access.

## Stop and fallback

Stop at the timebox or hard stop, or after about ten minutes of unsupported API guessing. Preserve the branch and explain the concrete blocker before changing tasks.

At 14:15 stop connector expansion and use T05 local immutable snapshots. Keep the connector ticket blocked/cut and publish no live-connect claim.

## Review and handoff

Review partner: Andrew + Eassa. Provide branch/commit, actual changed behavior, exact test commands/results, local/live/mock status, contract/config, remaining gaps, and next useful action. DONE requires acceptance and integration into trunk; otherwise REVIEW or BLOCKED.

No user PAT collection UI. User app access and GitHub installation entitlement are separate. Take only if ready before its cutoff; it is not an obligation to overbook Marc.

## Read only when needed

[Product architecture](../02_PRODUCT_AND_ARCHITECTURE.md) · [Tools and map](../03_TOOLS_AND_MAP.md) · [Build contract](../04_BUILD_CONTRACT.md) · [Security/tests](../05_SECURITY_AND_CI.md) · [Clock](../01_PHASE0_AND_CLOCK.md).
