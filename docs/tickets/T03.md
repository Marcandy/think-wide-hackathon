# T03 | Theme and the real product component workshop

**Product invariant:** Think-wide supplies cross-project evidence/decisions/briefs. It does not repair or execute source projects. Trusted bounded read-only analysis remains allowed. Exact references and current authorization govern every surface.

**Evidence rule:** Documentation/fixtures, installed APIs, local real handlers, live provider/host, and deployment are separate statuses. No pass without an actual result at the identified commit/config.

## Issuance

Suggested specialty: **Marc**. Actual owner: unclaimed until the PM assigns. Class: core.

Attempt timebox: **35 minutes**, including the first compatibility probe. Hard stop: **September 20, 2026, 12:30 EDT**, or earlier relevant feature cutoff. These bound effort; they are not promised completion times.

Prerequisite seed IDs: T01. Before READY, replace these with actual integrated commits/artifacts and record the contract version. No stale issue label is sufficient.

Writable areas: src/components/, src/routes/ workshop and shell, src/styles/. Confirm actual paths against the new checkout. Shared contract/lock/auth/infra changes need the designated owner and reviewer.

## Goal

Choose the tweakcn theme with Eassa and implement EvidencePair, ConnectionCard, ConstraintEditor, HandoffPreview and bounded composition against the reviewed contract design.

## Done means

Installed imports/build checked; theme in one reviewed token file; keyboard-usable correction input; synthetic workshop clearly labeled; no network/provider claim or arbitrary HTML/CSS from the model.

## Stop and fallback

Stop at the timebox or hard stop, or after about ten minutes of unsupported API guessing. Preserve the branch and explain the concrete blocker before changing tasks.

If Start/A2UI wiring fails its probe, retain the same React/Router shell and deterministic catalog. Mark the specific protocol unimplemented; do not add a second UI stack.

## Review and handoff

Review partner: Andrew. Provide branch/commit, actual changed behavior, exact test commands/results, local/live/mock status, contract/config, remaining gaps, and next useful action. DONE requires acceptance and integration into trunk; otherwise REVIEW or BLOCKED.

Do not wait for hosted WorkOS/GitHub. The workshop is not the product completion gate. Shared dependency additions go through the lock owner.

## Read only when needed

[Product architecture](../02_PRODUCT_AND_ARCHITECTURE.md) · [Tools and map](../03_TOOLS_AND_MAP.md) · [Build contract](../04_BUILD_CONTRACT.md) · [Security/tests](../05_SECURITY_AND_CI.md) · [Clock](../01_PHASE0_AND_CLOCK.md).
