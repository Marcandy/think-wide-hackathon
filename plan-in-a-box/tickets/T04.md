# T04 | Fixture and effect harness, not more scanner setup

**Product invariant:** Think-wide supplies cross-project evidence/decisions/briefs. It does not repair or execute source projects. Trusted bounded read-only analysis remains allowed. Exact references and current authorization govern every surface.

**Evidence rule:** Documentation/fixtures, installed APIs, local real handlers, live provider/host, and deployment are separate statuses. No pass without an actual result at the identified commit/config.

## Issuance

Suggested specialty: **Andrew**. Actual owner: unclaimed until the PM assigns. Class: core.

Attempt timebox: **40 minutes**, including the first compatibility probe. Hard stop: **September 20, 2026, 12:30 EDT**, or earlier relevant feature cutoff. These bound effort; they are not promised completion times.

Prerequisite seed IDs: T01. Before READY, replace these with actual integrated commits/artifacts and record the contract version. No stale issue label is sufficient.

Writable areas: tests/fixtures/, tests/helpers/, test configuration; CI test steps in coordination with Eassa. Confirm actual paths against the new checkout. Shared contract/lock/auth/infra changes need the designated owner and reviewer.

## Goal

Define independent A/B fixtures, exact byte/path cases, delayed provider responses, and state/job/provider counters. Adapt existing installed tools to the actual app.

## Done means

Runnable harness or clearly scoped tests against the first actual functions; positive and negative expectations; synthetic source metadata; provider stubs with observable effects. No mock that replaces the policy with an assumed deny.

## Stop and fallback

Stop at the timebox or hard stop, or after about ten minutes of unsupported API guessing. Preserve the branch and explain the concrete blocker before changing tasks.

Use the existing app runner and small direct harness. Do not block on extra scanners or hosted credentials; mark hosted verification NOT RUN.

## Review and handoff

Review partner: Eassa. Provide branch/commit, actual changed behavior, exact test commands/results, local/live/mock status, contract/config, remaining gaps, and next useful action. DONE requires acceptance and integration into trunk; otherwise REVIEW or BLOCKED.

Contract prose is enough to prepare independent cases. Integrate actual handlers after T02/T06 before claiming application-policy passes. The shared root install is owned by T01/T02.

## Read only when needed

[Product architecture](../02_PRODUCT_AND_ARCHITECTURE.md) · [Tools and map](../03_TOOLS_AND_MAP.md) · [Build contract](../04_BUILD_CONTRACT.md) · [Security/tests](../05_SECURITY_AND_CI.md) · [Clock](../01_PHASE0_AND_CLOCK.md).
