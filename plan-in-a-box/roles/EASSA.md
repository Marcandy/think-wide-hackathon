# Eassa: portable build brief

## Your specialty

**Integration captain and platform owner.** Start order: G0 → T01 → T02, then T06/T08 and bounded I01/I04 integration. These are ownership centers, not walls; once your accepted ticket is done, request another READY task.

Root/toolchain, public contract/generation, common principal/grant/revision admission, CI wiring, safe deployment and integration choices.

Do not take every GitHub API call, feature Convex function, UI component, or security test. Marc owns product behavior; Andrew can implement test infrastructure and trusted scanner fixtures. Review shared seams; let others review ordinary feature changes.

## The shared product contract

Think-wide helps people and agents navigate evidence across projects. It retains decisions and emits a scoped implementation brief. Specialist coding agents implement inside the target repository. Think-wide does not repair code, run target tests, install target dependencies, create branches/PRs, merge/deploy, or dispatch an autonomous coder. Trusted bounded Git/text/AST reads remain useful product tools.

Convex owns application state. Git evidence is identified by snapshot/blob/path/range and remains exact. The metadata tree is a navigable index, not another authored source tree. Generated views can change; human decisions and exact refs survive. One operation/policy boundary is shared by owned web, MCP, and any CLI.

The interface uses a reviewed tweakcn/shadcn theme, TanStack/React components and a closed semantic catalog. MCP remains a real agent-facing interface, not a future logo. A local demonstration does not prove remote ChatGPT/Claude.ai connectivity.

## Before writing

This package is documentation, not starter application code. Do not copy the old launch kit. Inspect the actual selected repository, its instructions, status, branch and installed dependencies. Wait for the permitted event window and real task authority. Do not reset or overwrite another person's work.

Obtain your issued GitHub ticket with actual prerequisite commits, contract version, writable areas, timebox, hard stop, reviewer and acceptance. Use one branch/worktree and one active writer. Root manifests/lockfile, canonical contract, auth profiles, schema composition and CI/deploy permissions are coordinated surfaces.

## What matters most in your lane

At each checkpoint inspect actual trunk, blocked core edge, available next task, current auth/source/host mode, and the recording. A folder or package export is not live integration. Hosted WorkOS/Convex settings remain unverified until tested.

## Clock and fallback

September 20, 2026, America/New_York: 12:30 shared local foundation; 13:30 protected evidence or isolated local mode; 14:15 two immutable repos; 15:00 complete loop + preserved recording; 15:30 brief; 16:00 feature freeze; 16:30 rehearsal. Follow the actual organizer build window and the chosen track's final deadlines. Do not silently extend a clock when starting late.

If authentication is blocking, fallback is loopback/stdio with operator-selected public/synthetic source only, no tunnel and no issue publishing. Never deploy a fixture principal or remove audience checks. Cutting a failed integration from today's critical path does not mark it implemented.

## Complete the ticket, then pull another

Send the coordinator commit/branch, changed behavior, actual checks/results, live/local/mock status, known gaps, and next useful action. A completion message is not evidence. REVIEW becomes DONE only after accepted integration into trunk and affected regression checks.

After roughly ten minutes of speculative API failure, stop, record the installed package/export and official source, and request READY work. Don't hide incompatibility behind `any`, disabled validation or parallel interfaces.

Ask the PM in ordinary language: "Next for Eassa", "Blocked <ticket>: <fact>", or "Done <ticket> at <SHA>, evidence <path>". These are messages, not installed commands. The coordinator reconciles on request; it is not an unattended scheduler.

## Relevant task seeds

[T01](../tickets/T01.md), [T02](../tickets/T02.md), [T06](../tickets/T06.md), [T08](../tickets/T08.md), [I01](../tickets/I01.md), [I04](../tickets/I04.md)

Full detail is available just in time in [Tools/map](../03_TOOLS_AND_MAP.md), [Contract/Convex/UI](../04_BUILD_CONTRACT.md), [Security/CI](../05_SECURITY_AND_CI.md), and [Clock](../01_PHASE0_AND_CLOCK.md). The issued ticket should include every important requirement needed for that slice rather than relying on an old chat.
